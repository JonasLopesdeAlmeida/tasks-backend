
pipeline {
    agent any //name of the agent
    stages{
        stage ('Build Backend') { 
            steps {
                 bat 'mvn clean package -DskipTests=true' 

            }   

        }
     
    }
}
